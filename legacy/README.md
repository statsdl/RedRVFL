# Legacy Scripts

This folder contains earlier research scripts retained for traceability.

The supported installable package lives under `src/redrvfl`. Code in this
folder is not included in the PyPI wheel and should not be treated as public
API.
